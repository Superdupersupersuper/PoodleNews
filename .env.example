PORT=4173

# Optional: official X API bearer token.
X_BEARER_TOKEN=

# Optional: low-latency compliant feed/provider endpoint for Truth Social posts.
# Expected response can be an array, { items: [...] }, or { statuses: [...] }.
TRUTH_SOCIAL_PROVIDER_URL=
TRUTH_SOCIAL_PROVIDER_TOKEN=
